# =============================================================================
# warp-xgen / apps/web environment template
# Copy to .env.local for local development. Vercel env vars are managed in the
# project dashboard (.env* is gitignored — only this template is committed).
# =============================================================================

# -----------------------------------------------------------------------------
# Model gateway
# -----------------------------------------------------------------------------

# OpenRouter — when set, all model calls route through OpenRouter instead of
# Vercel AI Gateway. Get a key at https://openrouter.ai/keys.
OPENROUTER_API_KEY=

# -----------------------------------------------------------------------------
# Better Auth (sessions)
# -----------------------------------------------------------------------------
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=

# -----------------------------------------------------------------------------
# Vercel OAuth (sign-in)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_VERCEL_APP_CLIENT_ID=
VERCEL_APP_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# GitHub App (repo access)
# -----------------------------------------------------------------------------
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY=
GITHUB_CLIENT_SECRET=
GITHUB_WEBHOOK_SECRET=
NEXT_PUBLIC_GITHUB_APP_SLUG=
NEXT_PUBLIC_GITHUB_CLIENT_ID=

# -----------------------------------------------------------------------------
# Database (Neon Postgres via Drizzle)
# -----------------------------------------------------------------------------
POSTGRES_URL=

# -----------------------------------------------------------------------------
# Redis / KV (rate limiting, ephemeral state)
# -----------------------------------------------------------------------------
KV_URL=
REDIS_URL=
RATE_LIMIT_TIMEOUT_MS=

# -----------------------------------------------------------------------------
# Sandbox
# -----------------------------------------------------------------------------
VERCEL_SANDBOX_BASE_SNAPSHOT_ID=

# -----------------------------------------------------------------------------
# Vercel runtime (typically auto-injected by the Vercel platform — leave blank
# locally unless emulating a deployment)
# -----------------------------------------------------------------------------
VERCEL_ENV=
VERCEL_URL=
VERCEL_PROJECT_PRODUCTION_URL=
NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL=
